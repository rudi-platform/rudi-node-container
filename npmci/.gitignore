# Root .gitignore ignores *-lock.json — allow package-lock.json
!package-lock.json

# Ignore symlinks generated by update-lockfile.sh
rudi-catalog
rudi-storage
rudi-manager
rudi-jwtauth

# Standard ignores
node_modules
